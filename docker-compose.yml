services:
  gomoku:
    build: .
    container_name: gomoku-game
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DATABASE_PATH: /data/gomoku.db
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data